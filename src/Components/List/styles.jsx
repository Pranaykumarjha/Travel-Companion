import { styled } from '@mui/material/styles';

export const FormControlSx = {
  margin: 1,
  minWidth: 120,
  mb: '30px',
};

export const SelectEmptySx = {
  mt: 2,
};

export const LoadingBox = styled('div')(() => ({
  height: '600px',
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
}));

export const ContainerBox = styled('div')(() => ({
  padding: '25px',
}));

export const MarginBottomSx = {
  mb: '30px',
};

export const ListSx = {
  height: '75vh',
  overflow: 'auto',
};
